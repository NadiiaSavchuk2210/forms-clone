/**
 * API Mutations Index
 * Central export point for all mutation hooks with optimistic updates
 *
 * Usage:
 * import { useCreateFormMutation, useSubmitResponseMutation } from '@/shared/api/mutations'
 */

export { useCreateFormMutation } from './forms';
export { useSubmitResponseMutation } from './responses';
