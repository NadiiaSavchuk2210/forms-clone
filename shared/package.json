{
  "name": "shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types for forms clone monorepo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "type": "commonjs",
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
